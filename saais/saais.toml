# SAAIS configuration — edit and restart the server (or it reloads per request).

[term]
# The most recent term that grade scrapes cover. Drives the "currently
# enrolled" section, stop-out detection, and the grade-encoding form.
current_ay = "2025-2026"
current_sem = "Second Semester"

[rules]
pass_threshold = 3.0          # numeric grades <= this pass
delinquency_ratio = 0.25      # failed >= this fraction of term units => delinquent
inc_years = 1                 # INC lapses to 5.00 after this many years
retakes_needing_attention = 3 # this many retakes => red status
max_units_regular = 24        # unit-load warning threshold in the advising assistant

[year_level]
# Year level is autocomputed from units earned. Format: "units=label" pairs,
# ascending, separated by ";". Empty = use the thresholds that ship with the
# student's curriculum workbook/record.
# thresholds = "0=1st year;36=2nd year;72=3rd year;110=4th year"
thresholds = ""

[server]
host = "127.0.0.1"            # local only; do not expose — the data is PII
port = 8000

[ai]
# Local AI advising chat (Students → a student → Advise → AI advising chat).
# Disabled by default; requires a local Ollama (https://ollama.com) install
# with the model pulled (`ollama pull qwen2.5:7b`). Only ever talks to `host`
# below — no other network calls are made, and chat transcripts are stored
# only inside that student's own record.json.
enabled = false
host = "http://127.0.0.1:11434"
model = "qwen2.5:7b"
timeout = 30                  # seconds to wait for a reply
